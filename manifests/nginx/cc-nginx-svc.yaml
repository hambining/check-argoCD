apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx
  name: cc-nginx-svc
  namespace: cc-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: nginx
