apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx
  name: cc-nginx-svc
  namespace: cc-frontend
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30080
  selector:
    app: nginx
  type: NodePort
